# nix-direnv: evaluates ../flake.nix and caches the dev environment.
# After `direnv allow`, entering eco-npm/ activates it automatically.
use flake ..#eco-npm

# After running `npm ci` in registry/, make verdaccio available on PATH.
PATH_add registry/node_modules/.bin
